name : Destroy
on: 
  workflow_dispatch:
jobs:
  destroy:
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v4
      - name: terraform destroy
        run: make dev-destroy